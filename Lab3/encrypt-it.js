(function() {
    
    window.onload = function() {
        let work = document.getElementById("encryptit");
        work.onclick = encrypt;
    };
    
    function encrypt() {
        let str = document.getElementById("text").value;
        
    }
    
    function shift(str) {
        return str
    }
    
    function randomized(str) {
        return str;
    }
    
    function size(str) {
        return str;
    }
})();